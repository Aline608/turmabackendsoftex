function imprimirLista(array) {
    for (let i = 0; i < array.length; i++) {
        console.log(array[i]);
    }
}

const minhaLista = ["Arroz", "Feijão", "Farinha", "Sal"];

imprimirLista(minhaLista);  
